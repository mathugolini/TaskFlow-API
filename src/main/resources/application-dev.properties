spring.application.name=taskflow-api

# Configuracoes de conexao com o banco de dados RDS MySQL
spring.datasource.url=jdbc:mysql://database-1.ch6e888y67e2.us-east-1.rds.amazonaws.com:3306/database-1
spring.datasource.username=admin
spring.datasource.password=CQqDSzhCf9nxOmrrehzp

# Configuracao do driver JDBC
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver

# Configuracoes do JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuracao do pool de conexoes HikariCP
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.pool-name=HikariCP

# Desabilitar open-in-view
spring.jpa.open-in-view=false

# Configuracao de Logs
logging.level.org.springframework.jdbc.core=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=DEBUG